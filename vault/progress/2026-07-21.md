---
title: Журнал — 2026-07-21
status: log
---

# 2026-07-21

Продовження сесії, розпочатої 16.07 (розмова тривала кілька календарних днів — попередні записи цієї сесії помилково дописувались у `progress/2026-07-16.md` замість нового файлу на кожну дату; з цього моменту повертаємось до правила "один файл на дату").

## Повне видалення каналів з панелі (не лише "вимкнути")

- Додано `pending_delete BOOLEAN` в `monitoring_channels` (схема + міграція на живій БД)
- `POST /channels/{id}/delete` (`app/admin/routers/channels.py`) — позначає `is_active=false, pending_delete=true`, публікує в Redis; не видаляє рядок одразу
- Нова `sync_pending_deletes()` в `app/userbot/channels.py`: якщо канал ще `join_status='joined'` — спершу `LeaveChannelRequest` (той самий урок з `@durov`, 2026-07-16 — не можна лишити підписку висіти), потім `DELETE` рядка незалежно від результату виходу. `sync_pending_leaves()` тепер виключає `pending_delete=TRUE` рядки з власного запиту, щоб не змагатись за той самий рядок
- `events_log` не чіпається — лог подій не прив'язаний FK до `monitoring_channels` (`source_channel` — просто текстове поле з marked ID), історія лишається навіть після видалення картки каналу
- UI (`_channels_content.html`): кнопка "Видалити" поруч із "Вимкнути" на кожному рядку (і активних, і вже вимкнених), з `confirm()` перед відправкою; поки видалення в черзі — бейдж "видаляється…" замість кнопок
- **Наскрізний тест:** видалення вже `left`-каналу (`@durov`) — миттєве, без потреби виходити (0 рядків після). Видалення активного `joined`-каналу (`@telegram`, повторно приєднаний для тесту) — лог підтвердив послідовність "Left channel @telegram before delete" → "Deleted channel card @telegram", рядок зник з БД за секунду через Redis pub/sub. Фінальний стан: 9 активних каналів, дашборд показує коректно

## Баг: колір ідентифікації каналу не відповідав аватарці (4 з 10 каналів)

Користувач помітив, що колір каналу "Повітряні Сили ЗС України" (`@kpszsu`) не відповідає ТЗ (дизайн-системі/логіці ADR-0009). Аватарка — темно-синій фон + жовтогарячий тризуб, збережений колір — червоний `#cb4d4d`.

**Причина, дві накладені проблеми (`app/common/avatars.py`):**
1. `img.resize((1,1))` без явного `resample`-фільтра використовував дефолтну бікубічну інтерполяцію — не справжнє усереднення пікселів, а інтерполяційний артефакт при зменшенні до 1×1
2. Навіть із коректним усередненням (`Image.Resampling.BOX`) цей конкретний логотип дає майже ахроматичний сірий піксель (`(76,72,72)`, `sat=0.027`) — контрастні насичені зони (синій фон + жовтогарячий символ) у сирому RGB-усередненні гасять одна одну. Hue з майже сірого пікселя чисельно нестабільний (шум у кілька одиниць RGB довільно перевертає значення), а `_legible()` все одно перетворює його на "впевнений" насичений колір — довільний і не пов'язаний з реальним зображенням

Перевірка всіх 10 завантажених аватарок показала: **4 з 10** каналів (`@sumygo`, `@kpszsu`, `@sumy_sumy1`, `@sumyliketop`) мали `sat < 0.15` у сирому середньому — тобто той самий баг, не поодинокий випадок.

**Виправлено:** `extract_avg_color()` тепер (а) використовує `Image.Resampling.BOX` для справжнього усереднення, (б) повертає `None`, якщо `sat < 0.15` — виклик тоді падає на вже наявний `fallback_color()` (детермінований hue з хеша назви), той самий механізм, що вже спрацьовував для каналів без фото взагалі. Скинув `avatar_color = NULL` для 4 постраждалих каналів напряму в БД, перезапустив обидва процеси — `@kpszsu` тепер `#8ecb4d` (стабільний процедурний зелений), не випадковий червоний.

Користувач запитав, чому фолбек — хеш назви, а не колір "з палітри" аватарки (напр. найнасиченіший піксель без повної квантизації). Пояснив: dominant-color-via-quantize вже свідомо відхилений в ADR-0009 через складність для 38×38px UI-акценту; фолбек лише перевикористовує наявний механізм. Запропонував легший проміжний варіант (найнасиченіший піксель) — **користувач відхилив, лишити як є**. Зафіксовано в ADR-0009, щоб не re-litigate без нового приводу.

## Підсумок сесії (16.07-21.07)

Vault-документація завершена станом на кінець сесії:
- [ADR-0009](../decisions/0009-avatar-color-hue-only.md) доповнено уточненням про near-gray-average edge case і фінальним рішенням користувача
- [architecture/overview.md](../architecture/overview.md) доповнено розділом про повне видалення каналів
- Усі зміни коду цієї сесії (delete-канали, фікс кольору аватарок) готові до коміту й пушу на GitHub
