# Секрети — ніколи не комітити
.env
.claude/settings.local.json
НЕ ЧИТАТИ!!!.txt

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# OS
.DS_Store
Thumbs.db

# Завантажені з Telegram аватарки каналів (похідні дані, не код)
app/admin/static/avatars/

# Рантайм-лог юзербота
userbot.log

# Graphify — згенерований граф залежностей коду (не джерело правди, легко
# перебудувати `graphify extract . --code-only`; вважаємо build-артефактом,
# не комітимо, щоб не роздувати репозиторій і не старіти в діффах)
graphify-out/
